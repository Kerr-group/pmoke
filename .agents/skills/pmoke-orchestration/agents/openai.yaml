interface:
  display_name: "pmoke Orchestration"
  short_description: "Run the 4-role board orchestration for pmoke under repository policy"
  default_prompt: "Use $pmoke-orchestration to triage, implement, review, merge, or reap pmoke Kanban work under repository policy; require explicit authorization before pushing, merging, deploying, releasing, or deleting branches."
