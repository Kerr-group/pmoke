---
title: 設定リファレンス
description: pmoke config schema version 4 の自動生成 field リファレンス。
---

{/* generated by cargo xtask docs-export */}

> 型付き config metadata registry からの自動生成。手動編集は上書き対象。

<ReferenceMetadata pmokeVersion="0.4.0" schemaVersion={4} />

[JSON Schema](https://kerr-group.github.io/pmoke/config.schema.json) · [`pmoke config explain`](../../cli/reference#pmoke-config-explain)

## `version`

| path | 型 | 必須状態 | 既定値 / 候補 | 説明・制約 |
| --- | --- | :---: | --- | --- |
| `version` | `integer` | 必須 | `4` | 設定スキーマ版 新規設定と移行済み設定で使用するバージョン 4。 **制約:** must equal 4 |
## `scope`

| path | 型 | 必須状態 | 既定値 / 候補 | 説明・制約 |
| --- | --- | :---: | --- | --- |
| `scope` | `table` | 必須 | - | オシロスコープ設定 登録済みモデルと通信 URI の組み合わせ。 |
| `scope.model` | `string` | 必須 | `DHO5108` | オシロスコープモデル instruments crate の登録名との一致。 **制約:** must be a registered oscilloscope model |
| `scope.connection` | `connection URI` | 必須 | `tcp://`, `visa:`, `gpib://` | オシロスコープ通信 URI DHO5108 で通常使用する直接 TCP/IP。Prologix 対象外。 **制約:** transport must be supported by the model and build features |
## `generator`

| path | 型 | 必須状態 | 既定値 / 候補 | 説明・制約 |
| --- | --- | :---: | --- | --- |
| `generator` | `table` | 任意 | `absent` | 任意のファンクションジェネレータ設定 trigger または自動測定系コマンドで必要な装置設定。 **制約:** required by trigger, autoshot, automeasure, and auto |
| `generator.model` | `string` | 条件付き | `WF1946B` | ファンクションジェネレータモデル generator table 使用時の必須登録名。 **制約:** must be a registered generator model |
| `generator.connection` | `connection URI` | 条件付き | `gpib://`, `prologix-tcp://`, `prologix-serial://` | ファンクションジェネレータ通信 URI native GPIB または Prologix TCP/serial の選択。 **制約:** transport must be supported by the model and build features |
## `data`

| path | 型 | 必須状態 | 既定値 / 候補 | 説明・制約 |
| --- | --- | :---: | --- | --- |
| `data` | `table` | 必須 | - | 取得データ方針 標準波形形式と任意スクリーンショット取得の設定。 |
| `data.output` | `string enum` | 必須 | `csv`, `raw`, `both` | 取得波形の保存形式 大規模取得向け raw、CSV 併記向け both。 |
| `data.input` | `string enum` | 必須 | `csv`, `raw`, `auto` | 解析波形の入力形式 利用可能な標準データを選ぶ auto。検証済み raw 優先。 |
| `data.screenshot` | `boolean` | 任意 | `false`; `true`, `false` | オシロスコープ画面取得 display transfer 対応モデル・通信方式での任意取得。 **制約:** DHO5108 capture requires TCP/IP or USB-TMC |
## `sensors`

| path | 型 | 必須状態 | 既定値 / 候補 | 説明・制約 |
| --- | --- | :---: | --- | --- |
| `sensors` | `array of tables` | 条件付き | `[]` | センサーチャンネル一覧 各 channel と単一 scale 方針の対応。 **制約:** channels must not overlap reference or lock-in signal channels |
## `sensors[]`

| path | 型 | 必須状態 | 既定値 / 候補 | 説明・制約 |
| --- | --- | :---: | --- | --- |
| `sensors[].channel` | `integer` | 必須 | - | センサーチャンネル番号 DHO5108 向け 1 以上 8 以下の番号。 **制約:** 1 &lt;= value &lt;= 8; must be unique across all channel roles |
| `sensors[].scale` | `inline table union` | 必須 | `{ factor = ... }`, `{ max_abs = ..., polarity = ... }` | センサー変換規則 直接係数または最大絶対値正規化の択一。 **制約:** factor and max_abs forms are mutually exclusive |
| `sensors[].scale.factor` | `float` | 条件付き | - | 線形センサー変換係数 積分センサー値への直接乗算。 **制約:** must be finite and non-zero |
| `sensors[].scale.max_abs` | `float` | 条件付き | - | 最大絶対値の正規化目標 測定最大絶対値に対応する正の目標値。 **制約:** must be finite and positive |
| `sensors[].scale.polarity` | `integer enum` | 条件付き | `-1`, `1` | 最大絶対値正規化の極性 max_abs 使用時の必須符号。 **制約:** must equal -1 or 1 |
| `sensors[].label` | `string` | 必須 | - | センサープロットラベル TOML literal string で指定可能な Matplotlib mathtext。 **制約:** must not be empty |
| `sensors[].unit` | `string` | 必須 | - | 変換後センサーの出力単位 表・プロット・解析保存ヘッダーでの単位。 **制約:** must not be empty |
## `pulse`

| path | 型 | 必須状態 | 既定値 / 候補 | 説明・制約 |
| --- | --- | :---: | --- | --- |
| `pulse` | `table` | 必須 | - | パルス背景区間 パルス前後のベースライン領域。 **制約:** background windows must not overlap |
| `pulse.background_before` | `inline time window` · `s` | 必須 | - | パルス前背景区間 有限な start と end の inline table。 **制約:** start &lt; end |
| `pulse.background_after` | `inline time window` · `s` | 必須 | - | パルス後背景区間 有限な start と end の inline table。 **制約:** start &lt; end |
## `reference`

| path | 型 | 必須状態 | 既定値 / 候補 | 説明・制約 |
| --- | --- | :---: | --- | --- |
| `reference` | `table` | 必須 | - | 光学参照信号の解析設定 reference channel、FFT 区間、fit 間引きの指定。 |
| `reference.channel` | `integer` | 必須 | - | 参照信号チャンネル DHO5108 向け 1 以上 8 以下の番号。 **制約:** 1 &lt;= value &lt;= 8; must be unique across all channel roles |
| `reference.fft_window` | `inline time window` · `s` | 必須 | - | 参照 FFT 時間区間 安定した参照信号区間を表す有限な start と end。 **制約:** start &lt; end |
| `reference.stride_samples` | `integer` · `samples` | 必須 | - | 参照 fit の間引き幅 計算量と時間詳細度の調整値。 **制約:** must be positive |
| `reference.window_samples` | `integer` · `samples` | 必須 | - | 局所参照 fit の窓幅 FFT 推定値周辺で使用する sample 数。 **制約:** must be positive |
## `lockin`

| path | 型 | 必須状態 | 既定値 / 候補 | 説明・制約 |
| --- | --- | :---: | --- | --- |
| `lockin` | `table` | 必須 | - | 数値 lock-in 設定 信号 channel、並列度、出力間引き、low-pass filter の指定。 |
| `lockin.signal_channels` | `integer array` | 条件付き | - | lock-in 復調対象チャンネル lock-in と後段解析で必要な 1 件以上の番号。 **制約:** each value must be in 1..=8; must be unique across all channel roles |
| `lockin.workers` | `integer` | 必須 | - | lock-in 並列 worker 数 物理 CPU 数付近を初期値とする並列度。 **制約:** must be positive |
| `lockin.stride_samples` | `integer` · `samples` | 必須 | - | lock-in 出力間引き幅 入力 sample rate を除算する sample 数。 **制約:** must be positive |
| `lockin.filter` | `tagged inline table` | 必須 | - | lock-in low-pass filter 設定 kind ごとに異なる許可 field。 **制約:** fields from other filter kinds are rejected |
| `lockin.filter.kind` | `string enum` | 必須 | `boxcar_legacy`, `fir_boxcar_enbw`, `fir_zero_phase`, `sync_iir_zero_phase` | lock-in filter アルゴリズム 同期平均と zero-phase IIR 平滑化を組み合わせる sync_iir_zero_phase。 |
| `lockin.filter.half_window_cycles` | `float` · `reference cycles` | 必須 | - | 参照周期単位の半窓幅 sync_iir_zero_phase の同期平均 full window の半分。 **制約:** must be finite and positive |
| `lockin.filter.cutoff_hz` | `float` · `Hz` | 任意 | `absent` | 絶対 low-pass cutoff cutoff 型 FIR/IIR filter で使用する周波数。 **制約:** must be finite and positive; mutually exclusive with cutoff_ref_ratio |
| `lockin.filter.cutoff_ref_ratio` | `float` · `f_ref ratio` | 任意 | `absent` | 参照周波数比の low-pass cutoff 0.02 で f_ref の 2% となる比率。 **制約:** must be finite and positive; mutually exclusive with cutoff_hz |
| `lockin.filter.stopband_atten_db` | `float` · `dB` | 条件付き | `60.0` | FIR stopband 減衰目標 fir_zero_phase 設計のみで使用する dB 値。 **制約:** must be finite and positive |
| `lockin.filter.sync_average_cycles` | `float` · `reference cycles` | 条件付き | `1.0` | 同期平均の full window 長 sync_iir_zero_phase 専用。対称 full window では half_window_cycles の 2 倍。 **制約:** 0 &lt; value &lt;= 100; must be finite |
| `lockin.filter.iir_order` | `integer enum` | 条件付き | `2`; `2`, `4`, `6`, `8` | IIR filter 次数 sync_iir_zero_phase 専用の偶数次数。 |
| `lockin.debug_output` | `boolean` | 任意 | `false`; `true`, `false` | lock-in debug artifact 出力 filter 調査時のみ有効化する大容量出力。 |
| `lockin.debug_label` | `string` | 任意 | `absent` | debug artifact の安全な suffix 短い ASCII filename-safe 文字列。 **制約:** 1-64 ASCII alphanumeric, '.', '_', or '-'; must not be '.' or '..' |
| `lockin.debug_overwrite` | `boolean` | 任意 | `false`; `true`, `false` | debug artifact 上書き方針 同名 debug 出力の置換許可。 |
| `lockin.snr_background_window` | `inline time window` · `s` | 任意 | `absent` | SNR 診断用背景区間 有限な start と end の任意 inline table。 **制約:** start &lt; end |
| `lockin.snr_signal_window` | `inline time window` · `s` | 任意 | `absent` | SNR 診断用信号区間 有限な start と end の任意 inline table。 **制約:** start &lt; end |
| `lockin.save_npy` | `boolean` | 任意 | `false`; `true`, `false` | lock-in 結果の NumPy 出力 標準 CSV に追加する NPY artifact。 |
## `phase`

| path | 型 | 必須状態 | 既定値 / 候補 | 説明・制約 |
| --- | --- | :---: | --- | --- |
| `phase` | `table` | 必須 | - | 位相回転設定 第 1 から第 6 高調波の offset。 |
| `phase.offsets` | `float or expression array` · `rad` | 必須 | - | 6 高調波の位相 offset pi/2 などの数式指定に対応する rad 値。 **制約:** must contain exactly 6 finite values |
## `kerr`

| path | 型 | 必須状態 | 既定値 / 候補 | 説明・制約 |
| --- | --- | :---: | --- | --- |
| `kerr` | `table` | 必須 | - | Kerr 角変換設定 校正 sensor、計算法、最終係数の指定。 |
| `kerr.sensor` | `integer` | 必須 | - | Kerr 校正用センサーチャンネル sensors 配列に存在する channel。 **制約:** must reference sensors[].channel |
| `kerr.method` | `string enum` | 必須 | `standard`, `harmonics` | Kerr 計算法 standard または harmonics 実装の選択。 |
| `kerr.factor` | `float` | 必須 | - | 最終 Kerr 変換係数 選択した Kerr 計算後の乗算値。 **制約:** must be finite |
## `plot`

| path | 型 | 必須状態 | 既定値 / 候補 | 説明・制約 |
| --- | --- | :---: | --- | --- |
| `plot` | `table` | 任意 | `defaults` | プロット生成設定 表示・保存、間引き、失敗方針の指定。 |
| `plot.mode` | `string enum` | 任意 | `save`; `off`, `save`, `interactive`, `both` | プロット出力 mode batch 向け save と Matplotlib window 向け interactive。 |
| `plot.max_points` | `integer` · `points per series` | 任意 | `100000` | プロット入力の最大点数 大規模取得時の Matplotlib 応答性調整値。 **制約:** must be positive |
| `plot.decimation` | `string enum` | 任意 | `stride`; `none`, `stride`, `min_max` | プロット間引きアルゴリズム 狭い極値の保持に適した min_max。 |
| `plot.on_error` | `string enum` | 任意 | `warn`; `warn`, `fail` | プロット失敗方針 解析成功を保持する warn と致命扱いの fail。 |
| `plot.output_dir` | `string` | 任意 | `absent` | 非推奨のプロット directory 互換 field version 4 で受理後に無視される値。artifact は標準 run layout。 **制約:** ignored with a warning |
